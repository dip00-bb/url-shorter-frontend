import React from 'react';

const Logo = () => {
    return (
        <div>
            <div className="text-2xl font-bold text-(--primary)" style={{ color: 'var(--primary)' }}>
                Shortify
            </div>
        </div>
    );
};

export default Logo;